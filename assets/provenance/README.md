# Alpha Asset Provenance

The curated unit animation packages under `assets/donor-units/` were copied from the local read-only donor repository `D:\godot\rpg`. Its generation report traces the sprite sheets to the local OpenDuelyst source checkout `C:\Users\qs\Desktop\游戏素材\duelyst-main (1)\duelyst-main`. The donor packages expose the stable animation cues `idle`, `move`, `attack`, `hit`, and `defeated`/`death`.

That upstream checkout's `README.md` states: “OpenDuelyst is licensed under the Creative Commons Zero v1.0 Universal license.” Its root `LICENSE` and `COPYING` both contain the CC0 1.0 legal code. An exact copy is retained as `OpenDuelyst-CC0-1.0-LICENSE.txt` (SHA-256 `A2010F343487D3F7618AFFE54F789F5487602331C0A8D03F49E9A7C547CF0499`).

No runtime resource points back to the donor repository.
