# 物品图标与基础音效

## 当前执行（2026-09-15）

用户已同意D26／D27的来源方案并明确“可以，执行吧”。本批为正式物品池6装备／15遗物补独立图标，接入战斗与UI基础音效；不改变物品效果，不扩展配乐／配音。机制内容深化仍沿bc-hero-roster与讨论候选推进，不能称本批已实现。

约束：仅main，保留现有未提交内容；不提交推送，不动真实存档，不使用Computer Use。D:\godot\rpg只读，明确选中的素材可复制入项目，不建外部路径运行依赖。Godot导入／构建／运行由主责串行。

主责负责21张图标生成、物品定义绑定、真实界面检查及集成文档；basic_audio Agent负责音源出处核对、固定音频池、真实战斗事实与UI事件接线、音量与限流、音频专项检查准备。共享文件修改先协调。音频只消费表现事实，不改变确定性模拟与随机序列。

图标：使用内置image_gen逐物品生成统一深色幻想插画；资源写入assets/ui/items，最终提示词与映射保存在该目录。大形体、无字、不同主体，检查小尺寸辨识与正式商店／背包显示。属性语义图标保留原职责。

音频：供体17份ogg尚未核实来源；优先核对与现有美术同源的OpenDuelyst许可，不依据文件名宣称试听。选定音频须保留来源／许可／加工记录；接入真实离弦、命中、技能、状态、死亡与UI事件，限制高频并发，遵守静音、暂停及场景清理。

验收：适量编译与资源加载检查；正式输入路径检查物品图标；音频事件路由、限流、静音和切换检查，必要时渲染录制。自动信号与数字检查不等于主观听感验收。最终记录实际覆盖和未验收项。

## 交付与验证（2026-09-15）

已接入21张独立物品插画与17种基础音效。图标逐件由内置image_gen生成，源图与完整提示词存于assets/ui/items；6装备／15遗物的Icon全部绑定新PNG，属性语义SVG保留。PNG无损导入256px并生成mipmap；UnitPortrait的静态fallback与装备槽使用线性mipmap，角色动画最近邻过滤不变。生成的霜羽徽章带alpha，保留原始透明通道。图标不代表物品机制已重做。

音源最终选择OpenDuelyst官方CC0素材，未使用供体来源未明的17ogg。17种加工声包含独立失败提示，来源commit／URL／原件和加工件SHA256／加工参数／许可与重建脚本归assets/audio。12声部池按同类并发、间隔及优先级限制，战斗／UI各自归属。弓箭在实际projectile_spawn／impact播放，挥击与近战成功命中分开；有效获盾／治疗、首次冻结、尸爆、召唤、真实死亡、胜负与按钮／购买／拒绝有反馈。无战斗模拟或RNG修改，临时清理不当作死亡，暂停／替换战斗会处理所属声部。

主责串行验证：

- `dotnet build my-team.csproj -maxcpucount:2 -v:minimal`：0警告／0错误；一次Godot无界面编辑器导入完成。
- 临时Godot图标检视页渲染全部21个当前物品定义，大图及32px小图可见；截图`.godot/ui-review/item-icons-sheet.png`，已查看。它是资源检视，不冒称正式背包交互。
- `tests/AudioFeedbackSmoke.tscn`：真实窗口、默认音频驱动，正式GameRoot与隔离存档，Continue→商店详情／购买、Continue→部署／开战／暂停／恢复通过；真实商店截图`.godot/ui-review/item-audio-shop.png`已查看，物品主体显示正确、按钮可见。检查同类限流、总声部上限、暂停不积压、切战停止、设置0静音及恢复正值解除静音。
- 专项输出`AUDIO_FEEDBACK_OK`，496128帧混音PCM、峰值0.4672。录音`.godot/ui-review/audio-feedback-main-flow.wav`开头约1秒含池压力样本，1–3秒静音，之后是实际主流程。Agent离线逐秒分析3–10秒仍有非零信号（RMS 0.0167–0.0655），不以开头样本代替正式输出。Master捕获器在最终音量前，不能拿PCM证明硬件静音；静音只报告总线状态检查。

范围与待体验：未逐个实际施放所有英雄技能，未做全局多分辨率／整塔回归；正式装备栏与奖励图标沿共享定义引用接入，本轮独立交互截图只覆盖商店。主观听感未试听验收，破盾／阵亡／失败是已记录的首版音色适配；声音舒适度、图标风格认可仍待用户。没有改真实存档、供体、研究原库，没有提交推送。

## UI 音色试听（2026-09-15）

用户反馈当前 UI 交互音一般，希望更清脆、类似“咔嗒咔嗒”。本轮生成轻鼠标点击／硬质按键／双段卡扣三版原创程序合成试听，归 `assets/audio/ui-click-study/`，附可重建脚本与来源／参数结果清单；单次 65／85／125 毫秒，48 kHz 单声道 PCM WAV，峰值 −9 dBFS。每版另附慢点三下与连点四下的试听文件。仅新增候选，尚未修改正式声音与运行时接线，目录排除 Godot 导入。已检查 WAV 解码格式、首尾归零与无削波；未进行主观试听验收或游戏内输入复验。本轮交付用于选择音色方向，不能视为用户认可或正式替换完成。

## 双音节 hover 接入（2026-09-15，后续音色调整见下）

用户随后要求在 hover 按钮时触发，并反馈原三版听起来都是“踏踏踏”，明确指定先 B 再 A 的双音节。本轮已将 B→A 组合为 D：两次起音相隔 80 毫秒，全长 145 毫秒。可重建源与试听保留在 `ui-click-study/`，正式副本 `assets/audio/ui/ui_hover.wav` 已绑定 `FeedbackAudio.tscn` 的 `ui_select`，使用无压缩 PCM 导入；旧 OGG 保留可恢复。来源与校验值归 `assets/audio/ui/source-manifest.json`。

`UiFeedbackBinding` 改为可交互按钮鼠标进入时请求选择音，停留不重播、禁用静音；键盘／手柄焦点进入沿用同一反馈，鼠标点击造成的焦点变化去重。普通鼠标点击不另播选择音，购买／失败结果音保留；动态创建的按钮、解绑、原有音量与限流机制沿用。

验证：最终低并发 .NET 构建 0 警告／0 错误；一次导入仅重导新 WAV。既有 `AudioFeedbackSmoke.tscn` 增补上述真实视口输入行为与单次悬停 PCM 捕获，最终输出 `AUDIO_FEEDBACK_OK`，573440 帧完整混音、峰值 0.3507；单次 hover 录音为 `.godot/ui-review/ui-hover-input.wav`。主菜单进入／停留／移出再进入／禁用、Tab 焦点、动态商店详情 hover 与点击去重、购买结果，以及已有暂停／静音／声部边界检查通过。检查过程修正了测试中原生鼠标瞬移和合成事件混用造成的退出事件干扰，以及 Tab 能落到禁用按钮的错误测试假设；未为这些测试假设修改产品逻辑。WAV 确认两段峰值之间存在空隙且无削波。主观“咔嗒”听感仍待用户试听，本记录不代表用户认可；未改真实存档、未提交推送。

## hover 改用单独 A（2026-09-15）

用户反馈 B→A 组合不好，要求“只有 A 试试”。已将正式 `ui/ui_hover.wav` 换为原始 `a_light.wav` 的相同字节副本，时长 65 毫秒，更新来源清单与当前说明；hover 接线、音量和限流沿用。复制保留的旧时间戳曾导致导入缓存仍为 145 毫秒，刷新目标修改时间并重导后，正式音频场景资源加载检查输出 `UI_HOVER_A_ASSET_OK duration=0.065`。本次仅替换音色，未重跑代码构建或完整输入流程；此前双音节混音录音不代表当前 A。等待用户试听，不记为音色认可。

## 当前：首页 hover、内部点击（2026-09-15）

用户已确认 A“音效可以”，指出触发太频繁，要求首页菜单 hover 响、内部界面点击才响。已由 GameRoot 显式注入 MainMenu 为唯一 hover 范围；内部静态与动态按钮改用 Pressed，悬停／焦点移动不请求声音，鼠标与键盘确认共用接受路径。首页焦点反馈、点击去重、禁用静音和购买／拒绝优先保留，A 音频资源未变。

低并发构建 0 警告／0 错误；音频专项渲染输入检查通过首页 hover／停留／重入／焦点／点击去重、内部设置页 hover／焦点静音与鼠标／键盘确认、切屏动作发声、动态商店详情点击、购买仅结果音及已有暂停／静音／声部检查。测试场景新增 authored SubViewport 隔离桌面原生鼠标事件，仍渲染正式 GameRoot 并通过真实 GUI 输入路径，不发射业务信号。最终 `AUDIO_FEEDBACK_OK`：621568 帧混音、峰值 0.3103；日志 `.godot/ui-review/ui-audio-trigger-scope.log`，内部点击 PCM `ui-internal-click-input.wav`。只认可用户明确接受的 A 音色，新触发频率的手动体验仍由用户观察；未提交推送或改真实存档。

## 单位动画帧音效（2026-09-15）

用户指出近战声音晚于攻击动画，要求可指定动画第几帧播放。原动作音由 attack／projectile_spawn 等战斗事件请求，动画有独立排队／播放生命周期；现在每个单位场景的 UnitAnimationComponent.FrameSounds 引用独立 UnitAnimationSound：逻辑动作、从 1 开始的帧号及 FeedbackSound（音源／音量／并发组）。同动作可设多条，实际进入帧时请求共享有界混音器；投射物计时动画跨帧补发一次，重播重置，暂停及中断不会积压旧声。配置的动作不再叠加旧事件动作音，实际命中、有效获盾／治疗等结果反馈保留。阵亡标记归被击败者；恢复先解除混音暂停，再恢复动画，避免首帧音丢失。没有改伤害／离弦的模拟结算时机。

55 个可普攻英雄／敌人／小兵均有独立 attack 标记，1 个禁用普攻单位排除。铁甲卫第 10 帧、追刃客第 8 帧已按实际逐帧图选动作开始／刀光帧，连弩手第 13 帧对应既有离弦进度。其他近战中段及远程离弦帧是可调整初值，未逐英雄完成听感校准；本批复用已许可声音，并非新增 55 种音色。配置表与永久编辑步骤归 content/audio/README.md。

实验室选择场上单位或库中原型→动画音效。独立 authored 弹窗实例化同一英雄场景，复制标记及嵌套声音资源；支持重播、暂停、前／后一帧静默查看、临时改帧、单独听音源和还原。修改／逐帧后继续从头重播；关闭释放实例和声音，重新打开恢复资源值。动作显示按全部帧可见范围并集适配。试听不是资源编辑器；永久配置仍在英雄场景／独立 .tres 中维护。

主责串行验证：最终低并发 build 0 警告／0 错误。AnimationFrameAudioSmoke 通过真实普通动画／不等时帧／排队／计时跳帧／暂停／取消／死亡／4倍速／循环检查，55 份生产配置直接加载通过，测试实例退出无泄漏。AudioFeedbackSmoke 通过实际主流程输入、动作去重／真实命中保留／阵亡归属、暂停和混音检查，输出 AUDIO_FEEDBACK_OK（622080 PCM 帧，峰值 0.3606），日志 animation-audio-routing.log。其后调整恢复顺序及预览并完成最终构建；AnimationAudioPreviewInputSmoke 通过真实实验室选取／点击入口／SpinBox 键盘改到第 2 帧／准确发声帧及混音／静默逐帧／关闭重开还原／共享资源隔离／宽动作各帧几何检查。输出 ANIMATION_AUDIO_PREVIEW_INPUT_OK；animation-audio-preview.png、-wide.png 已查看，无按钮遮挡或动作裁切，录音 animation-audio-preview.wav；上述产物均在 .godot/ui-review/。

自动检查证明配置时机与真实混音路径，不代表硬件延迟或主观听感通过。尚需用户按单位校准舒适音色与出手帧。仅 main，保留已有改动；没有提交推送、改真实存档或供体，不跑整塔／多分辨率全量回归。

## 密集战斗混音收敛（2026-09-15）

用户在受击音讨论后授权“优化一下”。已对普通挥击／离弦和命中拉开响度：前者 -12／-11dB、180ms短尾，后者 -17／-19dB、120／100ms短尾，末段淡出；原音源不重编码，单位音效帧保持。技能／破盾／爆炸放关键层，治疗／获盾／召唤／冻结／普通死亡放辅助层并降低重复频率。没有新增通用痛呼，也未对毒跳伤／反伤／每段附加伤害新增音效。

新增共享静态 FeedbackMixGroup 配置，四组分别为 attack、impact、support、highlight；每组独立跨cue最小间隔及同时声部数，同cue自身限制继续生效。组满且间隔允许时，高优先级可替换组内低优先级；拒绝即丢弃，不排队。相同时间的多目标爆炸会被限流，持续连锁可以按后续事件继续发声。关键声实际被接受后，将普通战斗声平滑压低7dB，短暂保持再恢复，UI不参与。声音计时使用表现层单调实时时钟，避免慢帧／模拟倍速提前截断新音；暂停冻结包络和短尾，清场完全复位。场景回退音和单位动画音共用分组资源。

验证：最终 dotnet build 0警告／0错误。AudioFeedbackSmoke 正式主流程真实输入／战斗／暂停／混音通过（combat-mix-main-flow.log，624128 PCM帧，峰值0.2563）；随后将包络计时改为单调实时时钟，专项 CombatAudioMixSmoke 在最终实现通过跨cue限流／并发／组内优先替换／新旧声部压低及恢复／UI不受影响／暂停冻结／短尾淡出／Clear／连续尸爆无补响。测试首次等待失败来自启动帧delta提前耗尽SceneTreeTimer，改为与准入一致的真实时间等待；暂停断言改在暂停命令完成后取样，没有放宽产品语义。最终 AnimationAudioPreviewInputSmoke 真实点击、改帧、输出PCM、关闭还原通过（combat-mix-animation-preview.log），发声帧和试听路径保持。相关diff空白检查通过；未跑整塔或多分辨率扫图。

当前配置与参数归 content/audio/README.md，可继续通过资源调节；没有修改原始音源／UI A音色、真实存档、供体或游戏战斗机制，没有提交推送。自动检查验证声音输出及混音行为，不以PCM峰值降低冒称主观更好听；密集战斗舒适度仍待用户实际体验。
