# 首批设计英雄与测试假人

2026-09-12 最新素材调整：用户指定反伤甲能力使用 `boss_borealjuggernaut` 素材。已将承载“荆棘甲”的 HC03 铁甲卫战斗动画与动态头像统一改为项目内该套 `SpriteFrames`；头像采用同素材已有的 Zoom 1.45、零偏移。保留 HC03 身份、嘲讽／增防／反伤能力、战斗属性和碰撞体型。仅调整两处表现资源引用，不修改原敌方 Boss 或共享图集。沿用用户“不用验证”要求，未构建、未启动 Godot 或运行体验检查。

## 目标、授权与边界

2026-09-11 用户要求清理未经设计的旧英雄、建立可布置的小兵假人，随后明确本轮一并接入 HC01 连弩手和 HC03 铁甲卫。此次授权覆盖所需代码、独立内容资源、实验室入口及必要验证；不等于合并全部讨论结论。只在 main 工作，不提交／推送，不修改研究原库，不覆盖原有未提交讨论及 web 改动。

## 内容与测试范围

- 正式可选／可招募名单只保留 HC01、HC03；HC02 暂停。旧 8 英雄和 24 旧招募单位退出正式内容。已有回归所需的旧配置隔离为测试夹具，不出现在游戏目录与实验室中。
- HC01：普攻与技能箭命中叠攻速，无设计层数上限；基础换目标清零，U01 测试升阶换目标保留，战后归零。主动发箭节奏读取攻速。3 箭、半普攻间隔、增量与伤害等均为本轮测试暂值。
- HC03：周围嘲讽并增防，挨普攻按当前防御反伤。范围、持续、增防和反伤系数为测试暂值。
- 假人提供静止承伤和普攻攻击用途，使用共同战斗结算，实验室可布置。后续新机制测试使用假人和合成配置，不再依赖旧英雄设计。
- 保留共享美术素材和既有敌人／装备／遗物系统；为清理修复其必要引用，不追加新玩法设计。旧真实存档及用户实验室预设不覆盖、不猜测替换英雄。

## 验收

- 正式内容完整发布；开始新局和实验室默认配置不引用旧英雄。
- 两名英雄独立场景可加载；HC01 普攻／技能箭增长、换目标、U01、战后重置；HC03 嘲讽／增防／普攻反伤和非普攻不递归；假人不会带旧英雄隐藏加成。
- 实验室真实输入布置、升阶测试、开始／重试路径；保存预设保留配置。
- 低并发构建和针对上述行为的隔离检查。不写入真实玩家存档，不把自动验证记为用户平衡认可。

## 当前进度／恢复点

2026-09-12 己方堵路修复：用户反馈单位不会绕过己方单位。旧路线只检查地形，密集静止身体依赖局部侧移，三个隔离场景均在 160 tick 后仍未接敌且小幅移动不断清零等待。现路线搜索、目标点择优和路径简化检查当前地面身体与明确跳跃落点预留；已有未来接敌点不作为墙。能绕行时沿该路线推进，不叠加把路线扭回障碍的侧移；临时完全封路仍保留地形计划与原跟随／等待路径。密集检查发现的极小位移问题也已修正：碰撞约束与提交统一丢弃同一阈值以下的位移，避免后车利用前车没有实际腾出的间隙。

验证：低并发构建零警告／错误；新增 `AlliedBodyNavigationContractSmoke` 的竖排、拥挤前排、贴边堵路由失败转为实际攻击（20／15／25 tick），每步检查身体与边界、静止友军未被推动。既有 `GameplayContractSmoke --movement-only` 覆盖地形绕行、9v9 密集流、迎面交通、6 人跟随链、目标与死亡清理、治疗接近、请求次序和带友军障碍的回滚重放，已通过；回滚测试同步当前 Emit 可选参数。9v9 的 36 tick 耗时约 0.68 秒，保留原性能上限。完整 `GameplayContractSmoke` 在进入移动用例前的护甲光环断言失败（预期 58.82353，实际 100），本次未改光环规则，不能宣称完整玩法回归通过。

沿 `MeleeContactVisualSmoke --allied-blockers` 使用正式 HC03、静止假人和 BattleScreen 完成 1600×900、x1 的隔离渲染，已查看起步／绕至列端／接近／交手画面；友軍保持原位，英雄停在中心距离 0.81 后产生普攻伤害。证据 `.godot/ally-navigation/`：`before.log`、`after.log`、`movement.log`、`gameplay.log`、`visual.log`、`visual/` 的 360 帧与轨迹。未触碰真实玩家／实验室存档，未做全流程或多视口测试；复杂阵容的玩家手感仍待反馈。

2026-09-12 最新修正：用户指出近战隔着非相邻格交手。原近战射程 1.2 是双方身体之外的可攻击间距，加上普通体型半径各 0.32，中心距离可达 1.84；现将正式近战定义及 `UnitDefinition` 默认值收至 0.25，普通体型中心需靠到约 0.89 格以内。沿用现有边缘测距、身体避让和连续接敌，未改远程射程或嘲讽半径 2.5。HC03、假人与既有近战敌人已同步；旧英雄夹具保留。

用户追加“调整完结束，不用验证”后停止后续检查与录制。追加要求之前已完成低并发构建（零警告／错误）和 `DesignedContentContractSmoke`：新增横向／斜向真实近战接敌检查，旧值在中心距离 1.52 时攻击复现失败，新值通过；反伤检查改为等待实际接敌首击。证据 `.godot/melee-reach/before.log`、`after.log`。`MeleeContactVisualSmoke` 已编写但未运行，不宣称渲染或手感验收通过。数值仍可按用户体验调整；本次完成后不继续扩大验证。

2026-09-12 最新完成：用户认可放箭修复，追加要求 HC03 嘲讽显示施放范围及敌人负面状态。已接入 `taunt_call`（脚下实际 2.5 半径边界＋两道外扩声压、约 0.9 秒收净）和 `taunted`（头顶右侧四瓣怒意符号、随真实状态持续）。新贴图通过内置 image_gen 制作，素材／提示词见 `assets/vfx/README.md`，动作参考 P01-S32。技能范围直接来自同一状态操作的查询，不复制另一套半径；持续标记消费真实 Status 快照、逐帧跟随可见单位，刷新不重建，多来源同类效果合并显示。到期／移除、目标死亡、有效嘲讽来源全部失效及换战立即清理。没有改变嘲讽、增防或反伤数值。

验证：低并发构建零警告／错误；`DesignedContentContractSmoke` 通过；新增 `DesignedTauntVisualSmoke` 覆盖实际施放只生成一次范围、圈内三人／圈外不受控、双轴斜投影半径、跟随与刷新保实例、到期及死亡快照清理、减少动态、真实视口鼠标暂停／恢复和结束清理。首次图形环境输入因坐标转换失败，测试改为显式视口局部坐标后通过；不是修改按钮业务逻辑。最终 Compatibility 1600×900 录制 390 帧，已查看施放／持续／结束画面；小标记启用 mipmap，位于血条右上侧以减少遮挡。用户观感仍待反馈。

证据目录 `.godot/taunt-vfx/`：`final.log`、`final-errors.log`（空）、`content.log`，`final/` 为最终渲染，`final.mp4` 为正常速度演示，`comparison.mp4` 为同画面上隐藏新特效／下显示新特效对照。对照使用同一当前战斗仅隐藏两个新效果，不冒称历史二进制；录制中含短暂的暂停输入检查，未记录输入函数等待帧。恢复：战斗实验室 → 铁甲卫嘲讽反伤，或特效预览室 → “嘲讽怒吼”“被嘲讽 · 怒意”。下一步按用户反馈调形状／显著性／节奏；其他内容仍按原边界。

2026-09-12 最新体验修正：用户指出拉弓尚未完成就已放箭，且箭飞行卡顿。已修正两个来源：此前攻击起手直接生成弹道；表现层又直接显示 10 Hz 战斗步的位置。当前普通箭和技能序列箭均先拉弓，实际释放后才生成弹道；飞行在相邻已知位置间逐渲染帧插值，使用战斗倍速与暂停时钟，碰撞仍由原固定步权威结算。命中／结束立即清理，不让装饰飞行越过已经发生的碰撞。

动作依据：复核当前 `assets/donor-units/f1_ranged/frames.tres` 的 15 帧攻击序列，零基第 12 帧手中箭消失，对应约 80% 进度；拆帧证据为 `.godot/arrow-timing/attack-frames.png`。沿现有侧视拉弓 → 松弦 → 沿目标方向直飞动作，只调整时序与位置采样，复用上一轮箭图，不新增美术或改动作平面。单位定义新增测试配置 `ProjectileWindupSeconds=0.5`、`AttackReleaseProgress=0.8`，应用到当前所有弹道普攻单位。前摇按攻速及技能连射倍率缩短，并以不超过攻击周期的释放比例为基础边界；向上对齐固定步后，动画使用同一时长，避免各用一套时钟。极高攻速仍允许多支箭同一步释放。

待释放箭归战斗实例并参与回滚；受控、释放时目标无效／超距／遮挡会取消该箭，不提前记伤害、普攻次数或回蓝。飞行中的箭保持既有语义。实际放箭会将动作推进到松弦姿势，满蓝技能延到下个步启动，避免在普攻释放的同一刻覆盖动画。基础与技能前摇、3 箭／65%／半间隔等仍是可调测试配置，没有据此获得最终平衡认可。

此次验证完成：低并发构建零警告／错误；`DesignedContentContractSmoke` 新增前摇无弹道／无伤害、释放时刻一致、控制与目标超距取消检查，原增长／连射／回滚检查通过；`RangedAttackContractSmoke` 的碰撞、穿透、遮挡、确定性与清理通过。`DesignedProjectileVisualSmoke` 使用正式 BattleScreen 完成正常与 x4 隔离渲染，确认普通／技能箭、插值中间位置、方向、暂停及清理；检查正常释放关键帧及 x4 画面。正常首批箭出生时角色已在第 12 帧，常见一次飞行的 38 个渲染帧含 31 个不同位置。出生到首个位置事实仍有至多一个战斗步的等待，结束立即服从权威，不声称零延迟或已诊断硬件帧率。

证据：`.godot/arrow-timing/content.log`、`collision.log`、`final.log`、`speed4.log`；正常完整录制 `final.mp4`，同视角同尺寸上旧／下新对照 `timing-comparison.mp4`，逐帧位置与动作记录在 `final/trace.csv`、`speed4/trace.csv`。对照旧源为上一轮 15 fps 录制，新源为 60 fps，保持正常播放时长；不能用编码帧率差异推断实际引擎帧率。下一步直接在实验室体验拉弓与放箭、普攻转连射及增长后的节奏；用户手感仍未验收。

### 前序：默认箭外观与英雄接入

2026-09-12 用户体验反馈：弩手普攻像光团、连射时看不到箭，要求远程默认使用箭矢。已将共享 `projectile` 资源由能量光团改为有箭头／箭杆／尾羽的箭，普攻和连射沿同一显示路径；移除该投射物的渐显，发射时立即可见。保留发射方向，让后续仅含位置的移动事实不会丢失朝向；投影后处理向左／斜向。未改箭数、发箭频率、飞行速度、碰撞或伤害。材料与图集观察归 `assets/vfx/README.md`，本次属于已有投射物动作的显示修正。

此次验证：低并发构建和 `DesignedContentContractSmoke` 通过；新增 `DesignedProjectileVisualSmoke` 使用当前 HC01 与静止假人、正式 BattleScreen 跑隔离战斗，确认普攻与技能期间的箭实例可见、出手无透明渐显，右／左／斜向及移动后朝向保持、减少动态、暂停与结束清理通过。1600×900 正常 x1 前后录制保留在 `.godot/arrow-fix/before.mp4`、`after.mp4`，同尺寸上旧／下新对照为 `comparison.mp4`；已查看普攻和连射阶段帧，并根据首次渲染将箭尺寸由 32 调至 40、略微提亮，以提高暗色棋盘上的辨识。这里只确认实现与渲染证据，用户观感仍待反馈。

2026-09-12：本批实现与针对性验证完成，待用户体验反馈。下一步从主菜单 → 战斗实验室，选择下列预设，按 HC01／HC03／U01 稳定编号修订；不自动展开完整升阶系统或统一合并其他讨论。

- 正式发布名单只有 HC01、HC03 与三类测试假人。两名英雄、每种假人均有独立 `.tscn`，定义、肖像与技能使用现有 Resource／组件组合。旧配置移至 `tests/fixtures/legacy-roster/`；正式资源没有引用该夹具。
- 新局默认两名设计英雄，选中英雄不会在初始同伴中重复。正式招募只出设计英雄，假人在资源池、招募命令与活动征程校验中被拒绝。旧体验切片也已适配两名英雄与两项招募候选，未重做关卡平衡。
- 实验室默认双方可拖入假人；内置“默认配置”“连弩手叠速”“连弩手升阶换目标”“铁甲卫嘲讽反伤”四个预设。选中弩手显示 U01 开关，实例覆盖可随预设保存；战斗详情显示防御与追猎层数。修复退出界面时解除按钮订阅的节点访问报错。
- 连射使用公共投射物与碰撞链；叠速、序列、小数攻击进度归战斗实例并参与事务回滚。技能命中与普攻命中事件分开，避免技能箭取得普攻回蓝或触发铁甲反震。成长普攻允许一个固定步内多次发射，不使用旧攻速 1000 上限。
- 嘲讽为公共带来源状态；铁甲反震复用常驻状态与事件反应，防御增益复用属性系统。沿正常寻路接敌；不拉位、不重定向自动技能与已发弹道，最新有效嘲讽来源优先。

## 测试暂值与保留限制

数值单一当前稿见 [content-drafts.md](../../design-discussion/04-content-validation/content-drafts.md#当前可测试版本2026-09-12)：HC01 每层 +0.04 攻速、主动 3 × 65% 攻击／半普攻间隔；HC03 半径 2.5、3 秒、+18 防御、100% 当前防御物理反伤。资源与界面明确标记测试稿。这不是最终平衡或全部细则认可。

- U01 目前仅实验室单实例测试开关；正式征程使用基础版，尚无培养取得 U01 的流程。
- 英雄能力层数不跨战；预设仅保存初始设置与是否测试 U01，不保存战斗叠层。
- 三种假人暂值：静止承伤生命 5000、不走不攻；近战生命 800、攻击 12、间隔 1 秒；远程生命 500、攻击 10、间隔 1 秒、射程 5.2。全为测试用途，无旧模板被动或羁绊。
- `ability_raise_dead`、`ability_paid_reinforcement`、`item_clockwork_seed` 原引用已删除单位，现临时召唤近战假人以保持既有入口合法；它们尚未经此次内容设计。VFX 的 Rend 预览改用仅含表现绑定的预览资源，不再引用旧英雄技能。
- 共享素材、既有敌人／物品、其他当前机制保留；外层、完整升阶与其他讨论规则没有因此对齐。旧英雄真实存档／预设不自动替换为新英雄，现有未知内容校验负责拒绝；本轮未执行真实存档迁移或覆盖，未声称旧档迁移已验收。

## 实际验证（2026-09-12）

- `dotnet build my-team.csproj -maxcpucount:2 -v:minimal`：通过，零警告／错误。
- `DesignedContentContractSmoke`：正式内容完整发布、两个新局起点、假人招募拒绝、四个预设、旧体验入口启动；普通／技能箭叠速、基础与升阶换目标、重开归零、超过旧攻速限制／单步多发；三箭节奏随攻速、技能无普攻回蓝与反伤；嘲讽目标／增防到期／护盾命中反伤／当前防御公式／反伤不递归；施法故障回滚序列与法力。通过。
- `DesignedContentInputSmoke`：真实视口鼠标拖入英雄与敌方假人、选中、点击升阶开关、开始、恢复默认和预设 DTO 往返。通过；OpenGL 渲染截图 `.godot/designed-content-input.png` 已检查布局，无明显重叠。此项不代表战斗手感已验收。
- `BattleLabAppFlowContractSmoke`：隔离存档下主菜单 → 实验室 → 实战 → 战报 → 返回配置 → 菜单／重入及中途重置的真实输入流程。通过，使用当前正式名单。
- `ContentContractSmoke`：旧夹具发布、结构／生命周期失败拦截、可扩展目录与内容 id 源码门禁通过；日志中的 `CONTENT_GATE_*_FAILURE` 为刻意注入的反例，不是实际正式内容错误。
- `ExperienceSliceContractSmoke` 与 `ExperienceSliceInputSmoke`：冻结旧内容的兼容回归、两战结算、奖励一次性、装备转交／替换、检查点重试与鼠标／键盘选择通过；新正式切片启动另由新内容测试覆盖。
- 静态检查：正式 content／scenes 及旧夹具的 `res://` 引用缺失为 0，正式资源引用旧夹具为 0；`git diff --check` 通过（仅换行提示）。无提交或推送，仍为 main，保留原有讨论和 web 改动。

保留日志：`.godot/designed-content-final.log`、`.godot/content-contract-final.log`、`.godot/experience-compatibility-final.log`、`.godot/experience-input-final.log`。手动观察点与复现命令见 [测试用例](../../docs/testcases/battle-lab.md#设计英雄与假人2026-09-12)。本次没有完整征程平衡验证；玩家负责节奏、乐趣与强度验收。
