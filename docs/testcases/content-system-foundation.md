# 内容基础能力验收入口

## 开局与分阶供给专项（2026-09-16）

`tests/RecruitmentOpeningContractSmoke.tscn` 使用正式包和内存存档。headless覆盖128个开局种子、阶段边界/统计、持有与后备排除、耗尽/不足、保存失败、读档保留及旧名册保留；渲染模式另用实际鼠标/Tab/Enter驱动正式菜单、6选2、取消、第三人限制、返回/读档继续、确认失败重试和首战备战，并保存截图。不得用其结论代替整局节奏与平衡试玩。

玩家观察：重新开始一局是否只见6名低阶候选；选2人后是否进入首战备战；中期是否更容易接上核心；后期高阶剩余很少时，候选缩减是否仍有值得选择的补强。配置和实际结果见[活动任务](../../work-items/active/recruitment-opening-and-tiers.md)。以下通用内容任务的历史不自动试玩边界不替代本专项最新授权。

## 统一伤害专项（2026-09-21）

用户已授权修复现有实现。本专项使用隔离战斗与测试存档验证，不受下方旧基础任务未自动执行边界限制。运行证据归[活动任务](../../work-items/active/unified-damage-defense.md)。

- `AbilityFoundationContractSmoke`：各类公式都输出100原始值，公共增幅后120；目标防御100/7时普通伤害60、真实伤害120。无盾、部分盾、全吸收及致死过量均校核生命、盾量、有效伤害和来源。
- `EnemyPiercingContractSmoke`：正式贯穿箭与光束均使用同一防御曲线，提高防御继续降低光束伤害，同时保留飞行／瞬发与命中衰减。
- `ContentCarrierGrantContractSmoke`：来源资格和普通／真实筛选仍独立；`ContentPrimitiveContractSmoke`：法强／防御快照及退休配置拒绝。
- `CurrentUiInputCapture --tooltip-icons`：真实鼠标／键盘／悬停／拖放路径，法强与伤害值着色，真实伤害优先匹配；图例和技能说明不得再展示双防类别。用户试玩观察原法术技能对高防御敌人的强度，未自动调整系数。

## 原基础任务边界

本轮按用户要求不自动试玩、不启动整局战斗或真实存档迁移。下面的 smoke 是已编写的隔离契约入口，是否已编译/执行以 `work-items/active/content-system-foundation.md` 为准；存在文件不等于行为通过。

## 独立契约入口

| 入口 | 保护的行为 |
| --- | --- |
| `tests/ContentPrimitiveContractSmoke.tscn` | 有界公式与循环拒绝、缺失上下文拒绝、编译快照不随资源编辑改变、目标范围/标签/生命比例/数量筛选、效果来源与独立使用额度、法强/防御作者入口、退休枚举配置拒绝 |
| `tests/AbilityFoundationContractSmoke.tscn` | 正式命中/阵亡触发、周期自动选取、被动授予撤销、反应失败回滚、固定／攻击／法强／混合公式的统一防御、普通／真实伤害共同修饰／护盾／过量伤害统计／击杀归属、初始授予与生命归一顺序、召唤不覆盖既有生命、正常限流 |
| `tests/ContentCarrierGrantContractSmoke.tscn` | 相同source不同grant隔离、撤销/替换失败恢复、羁绊成员变化/下降、装备受击、状态来源与伤害类型过滤、配置上下文门禁 |
| `tests/RunDecisionContractSmoke.tscn` | 选择与成本/收益/机会原子提交、失败重试、待决恢复、终局Meta失败/幂等、迁移歧义拒绝、初始人数布局、条件不满足的选项可解释 |
| `tests/RecruitmentUniquenessContractSmoke.tscn` | 正式英雄池中的持有／后备排除、同批去重、起始同伴、确定性、耗尽可跳过与恢复、重复领取拒绝、组合分支、保存失败回滚、物品重复策略保持 |

这些入口应使用测试构造或内存存档，不改生产的英雄/装备/遗物资产。原有 AbilityStatusContractSmoke 的被动样例改为合法常驻状态，不能继续把瞬时操作当成已支持的被动语义。

## 玩家正式流程观察点（待验收）

1. 招募、商店与战后奖励仍显示原有英雄动画肖像/装备卡。混合选项顺序稳定，鼠标悬停及键盘聚焦能查看完整条件、代价、概率和失败结果。
2. 战胜后不领奖退出，再继续：应恢复同一奖励机会；领取/跳过之后不得再次领取。奖励成功后的装备分配仍可用。
3. 休息/事件选择的全部收益与节点推进应同时生效；使用隔离故障存储拒绝保存时，机会和资源都保留。
4. 合成技能/装备/羁绊配置触发与撤销后，实际属性和伤害事实应对应变化；看得到说明不等于触发已执行。
5. 旧 v5 非战斗待决存档如无法证明已领奖状态，应明确拒绝并保留原件；不要为了验证去覆盖真实玩家存档。终局保存失败应停留可重试状态。
6. 普通招募不显示场上或后备已有英雄，同批不重复；新招英雄进入后备后，下次候选同样排除。退出并恢复待决机会不重抽；合法池耗尽可用继续按钮退出。材料培养、阶段推荐与6选2不在本次检查范围。

2026-09-16普通招募专项的执行证据见[活动任务](../../work-items/active/ordinary-recruitment-uniqueness.md)：正式包专项已通过；历史RunDecision／FormationDeployment入口在旧包资源加载门禁受阻，不能记为通过。上文原任务的未运行边界保留，不覆盖此次专项记录。

不要求本轮做多分辨率截图扫查、整局自动推进或真实玩家数据故障注入。
